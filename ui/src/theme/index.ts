import { createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#006484',
      dark: '#868b8f'
    },
    secondary: {
      main: '#d1711a',
    },
  },
})

export default theme
